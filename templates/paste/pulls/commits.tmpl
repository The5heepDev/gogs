{{template "base/head" .}}
<div class="repository view issue pull commits">
	{{template "paste/header" .}}
	<div class="ui container">
		<div class="navbar">
			{{template "paste/issue/navbar" .}}
			<div class="ui right">
				<a class="ui green button {{if not .PullRequestCtx.Allowed}}disabled{{end}}" href="{{.RepoLink}}/compare/{{.BranchName}}...{{.PullRequestCtx.HeadInfo}}">{{.i18n.Tr "repo.pulls.new"}}</a>
			</div>
		</div>
		<div class="ui divider"></div>
		{{template "paste/issue/view_title" .}}
		{{template "paste/pulls/tab_menu" .}}
		<div class="ui bottom attached tab pull segment active">
			{{template "paste/commits_table" .}}
		</div>
	</div>
</div>
{{template "base/footer" .}}
